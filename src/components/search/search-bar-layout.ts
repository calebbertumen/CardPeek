/**
 * Horizontal alignment for every search bar shell  -  matches {@link SiteHeader} inner row:
 * `max-w-6xl` + `px-4 sm:px-6` so the bar lines up with the logo and nav actions.
 */
export const searchBarWidthClassName = "mx-auto w-full max-w-6xl px-4 sm:px-6";
